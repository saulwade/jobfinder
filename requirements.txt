# Core
requests>=2.31
pyyaml>=6.0
python-dateutil>=2.9
feedparser>=6.0          # RSS sources (We Work Remotely, etc.)

# CV parsing / generation
pypdf>=4.0               # read existing PDF CV
markdown>=3.6
weasyprint>=62           # markdown/HTML -> one-page PDF

# AI matching + tailoring
anthropic>=0.40          # Claude API

# Dashboard (Phase 4)
streamlit>=1.40

# Scheduler (Phase 5)
apscheduler>=3.10

# Env
python-dotenv>=1.0
